// Helpers and logic for Training & Engagement Data

export const buildTrainingEngagementData = ({
    literacyLevel,
    preferredLanguage,
    areasOfInterest
}) => ({
    literacyLevel,
    preferredLanguage,
    areasOfInterest
});

